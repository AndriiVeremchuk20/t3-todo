const Loader = () => {
  return (
    <div className="flex h-screen w-full animate-pulse items-center justify-center rounded bg-neutral-600">
      <div className="font-semibold text-white">Loading..</div>
    </div>
  );
};

export default Loader;
